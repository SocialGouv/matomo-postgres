minimum-release-age=4320

# Fail if pnpm trust evidence is downgraded (supply-chain hardening)
trust-policy=no-downgrade

# Block dependency build scripts by default (allow only explicitly approved deps)
strict-dep-builds=true